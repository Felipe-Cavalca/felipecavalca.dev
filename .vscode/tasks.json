{
    "version": "2.0.0",
    "tasks": [
        // Scripts do back
        {
            "label": "back docker-compose up",
            "type": "shell",
            "command": "docker-compose",
            "args": ["-f", "api/docker-compose.yml", "up", "--build", "-d"]
        }
    ]
}
